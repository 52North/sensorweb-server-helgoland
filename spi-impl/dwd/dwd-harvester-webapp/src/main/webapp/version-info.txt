General Infos:
==============
project = ${artifactId}
version = ${git.describe}

builtAt=${git.build.time}

Details:
========
git.branch=${git.branch}
git.commit=${git.commit.id}
git.remote=${git.remote.origin.url}
git.dirtyWorkdir=${git.dirty}